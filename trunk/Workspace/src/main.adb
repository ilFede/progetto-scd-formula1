with Subsegment.Segment;
with Ada.Text_IO;
procedure Main is
   --use Pilot;
   use Ada.Text_IO;
   --Easy : Race_Pilot (1);
   begin
      -- CircuitTrack.CircuitConfig;
      Put_Line("dadsdsad");
      -- Circuit_Config;
      Put_Line("dadsdsad");
      --test := new Race_Pilot (8);
end Main;
